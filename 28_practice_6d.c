#include <stdio.h>

int savg(int *a, int *b);

int main(){
    int a,b;
    printf("Enter the value of a : ");
    scanf("%d",&a);
    printf("Enter the value of b : ");
    scanf("%d",&b);
    int s=savg(&a,&b);
    printf("The values after calling the function is : %d\n",s);
    return 0;
}

int savg(int *a, int *b){
int s(int *a, int *b){
    int z= *a + *b;
    return z;   
    }
int avg(i)
    int f = (*a + *b)/2;
    return f;

}