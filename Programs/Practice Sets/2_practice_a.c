#include <stdio.h>

int main(){
    int b;
    printf("Enter the no. : ");
    scanf("%d",&b);
    printf("The divisibility test returns : %d\n",b%97);
    printf("If the result is 0 the no. is divisible\n");
    printf("If the result is not 0 the no. is not divisble\n");
    printf("%d",3*2/3-3+1);
    return 0;
}